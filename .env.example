# Server Configuration
PORT=3001

# OpenRouter API Configuration (Optional - can be configured per LLM in UI)
OPENROUTER_API_KEY=your_openrouter_api_key_here

# n8n Webhook Configuration (Optional - can be configured per LLM in UI)
N8N_WEBHOOK_URL=your_n8n_webhook_url_here

# LM Studio Configuration (Optional - can be configured per LLM in UI)
LMSTUDIO_WEBHOOK_URL=http://localhost:1234/v1/chat/completions
